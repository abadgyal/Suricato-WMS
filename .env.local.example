# Copia este fichero a `.env.local` (gitignored) y rellena el valor.
# Cadena de conexión directa/pooler a la BD de Supabase (Project Settings →
# Database → Connection string). La usa el runner de tests scripts/db-test.mjs.
# NUNCA la comitees: contiene la contraseña de la base de datos.
SUPABASE_DB_URL=
